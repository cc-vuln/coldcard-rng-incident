# The capture browser is deliberately the only part of this project with a
# dependency beyond the standard library and gallery-dl. Everything under
# scripts/ stays stdlib-only so the archive tooling still runs in ten years.
playwright>=1.40
