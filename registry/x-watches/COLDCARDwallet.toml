# registry-order: 75

# ---------------------------------------------------------------- X / social
# Captured separately via scripts/capture-x.sh (gallery-dl). Listed here so the
# timeline generator knows about them and so we have a record of what mattered
# even if capture fails.

# Discovery watches are operational leads, not published sources. The manual
# `just discover-x` command reads one shallow official-API timeline per handle,
# queues new permalinks for LLM assessment, and leaves capture to ingest-x.py.
# First contact establishes a baseline unless --queue-initial is explicit.

[[x_watch]]
handle = "COLDCARDwallet"
org = "Coinkite"
since = "2026-07-29"
why = "Official COLDCARD account. Advisories, investigation updates and corrections may appear here first."
