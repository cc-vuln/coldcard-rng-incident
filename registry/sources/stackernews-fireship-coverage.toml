# registry-order: 774

[[source]]
id = "stackernews-fireship-coverage"
title = "Fireship covered the coldcard hack 🤣"
url = "https://stacker.news/items/1541274"
published = "2026-08-06"
org = "Stacker News"
kind = "community-discussion"
tier = 3
watch_until = "20260810T094959Z"
min_chars = 100
capture = "http"
fetch_url = "https://stacker.news/api/graphql"
fetch_post = '{"query": "{ item(id: 1541274) { title text createdAt user { name } comments { comments { text createdAt user { name } } } } }"}'
json_pretty = true
required_text = ['"title"']
note = """
justin_shocknet linking Fireship's video coverage of the COLDCARD incident, which is not separately registered or captured here. A link post held as minor colour marking the incident's arrival in mainstream tech-media commentary. Captured through the site's public GraphQL API: the rendered pages crash the capture tab, and the API answers POST from this host. The query fixes the captured surface to the item's title, text and two levels of comments, each with author and absolute timestamp.
"""
