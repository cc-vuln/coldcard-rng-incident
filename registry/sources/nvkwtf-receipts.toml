# registry-order: 290

[[source]]
id = "nvkwtf-receipts"
title = "nvk.wtf receipts index"
url = "https://nvk.wtf/receipts"
org = "nvk.wtf"
kind = "aggregator"
tier = 3
capture = "browser"
min_chars = 500
# "Screenshots of NVK's posts" was the standfirst until the operator rewrote
# it (observed 6 Aug 2026, widening the page to the COLDCARD account and
# Coinkite staff). The marker outlived the sentence and blocked 36 consecutive
# polls of a page that had materially changed. Anchor on the heading and the
# submission hashtag, which survived the rewrite and are specific to this site.
required_text = ["Receipts", "#wtfnvk"]
note = """
Screenshots of NVK posts, some already deleted, kept with dates and, where one
exists, a link to an independent archive; solicits further screenshots via the
#wtfnvk hashtag. Same operator and stance caveats as nvkwtf-articles. The
screenshots are third-hand material: anything that matters here is captured
from the original post or the independent archive it links before it backs a
claim.
"""
