# registry-order: 330

[[source]]
id = "stackernews-legal-responsibility"
title = "Is Coldcard legally responsible for the lost of the funds?"
url = "https://stacker.news/items/1537215"
published = "2026-07-31"
org = "Stacker News"
kind = "community-discussion"
tier = 3
min_chars = 400
capture = "http"
fetch_url = "https://stacker.news/api/graphql"
fetch_post = '{"query": "{ item(id: 1537215) { title text createdAt user { name } comments { comments { text createdAt user { name } } } } }"}'
json_pretty = true
required_text = ['"title"']
note = """
Bitcoiner1 asking whether Coinkite is legally responsible for the lost funds. Early community framing of the liability question; no legal reasoning here is endorsed. Captured through the site's public GraphQL API: the rendered pages crash the capture tab, and the API answers POST from this host. The query fixes the captured surface to the item's title, text and two levels of comments, each with author and absolute timestamp.
"""
