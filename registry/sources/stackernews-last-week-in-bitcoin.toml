# registry-order: 360

[[source]]
id = "stackernews-last-week-in-bitcoin"
title = "The COLDCARD Incident — Last Week in Bitcoin (Jul 27 - Aug 02)"
url = "https://stacker.news/items/1539442"
published = "2026-08-03"
org = "Stacker News"
kind = "community-discussion"
tier = 3
min_chars = 100
capture = "http"
fetch_url = "https://stacker.news/api/graphql"
fetch_post = '{"query": "{ item(id: 1539442) { title text createdAt user { name } comments { comments { text createdAt user { name } } } } }"}'
json_pretty = true
required_text = ['"title"']
note = """
tuma linking 'The COLDCARD Incident, Last Week in Bitcoin (Jul 27 - Aug 02)'. Link post to a weekly roundup; held for the discussion and as a pointer to secondary coverage. Captured through the site's public GraphQL API: the rendered pages crash the capture tab, and the API answers POST from this host. The query fixes the captured surface to the item's title, text and two levels of comments, each with author and absolute timestamp.
"""
