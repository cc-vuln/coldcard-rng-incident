# registry-order: 323

[[source]]
id = "stackernews-optech-416-thread"
title = "COLDCARD, CLN disclosures, zk proof of reserves - Bitcoin Optech Newsletter #416"
url = "https://stacker.news/items/1536932"
published = "2026-07-31"
org = "Stacker News"
kind = "community-discussion"
tier = 3
min_chars = 400
capture = "http"
fetch_url = "https://stacker.news/api/graphql"
fetch_post = '{"query": "{ item(id: 1536932) { title text createdAt user { name } comments { comments { text createdAt user { name } } } } }"}'
json_pretty = true
required_text = ['"title"']
note = """
schmidty linking Bitcoin Optech newsletter #416, which warned about the COLDCARD vulnerability. The newsletter itself is registered as optech-416; this source holds the Stacker News discussion of it. Captured through the site's public GraphQL API: the rendered pages crash the capture tab, and the API answers POST from this host. The query fixes the captured surface to the item's title, text and two levels of comments, each with author and absolute timestamp.
"""
