# Routes retired by the August 2026 content consolidation.
#
# Every published URL must still resolve: docs/design/site-information-architecture.md
# lists that as an acceptance criterion, and these paths are indexed and cited.
# Cloudflare Pages serves _redirects only when no static file matches, so an
# entry for a page that still exists is inert.
#
# Merged pages keep their content behind a disclosure rung, and the fragment
# opens that rung on load, so a citation lands on the same material.

/how-it-broke/promised/   /how-it-broke/#documented-design   301
/how-it-broke/the-fix/    /how-it-broke/#the-fix             301
/risk/dice/               /risk/mitigations/#dice            301
/risk/passphrase/         /risk/mitigations/#passphrase      301
/risk/multisig/           /risk/mitigations/#multisig        301
/risk/migrating/          /risk/moving-funds/#threshold-wallets 301
/record/disclosure/       /record/timeline/                  301
/response/precedent/      /response/#precedent               301
/schemas/source-register-v1   /schemas/source-register-v1.json  301
